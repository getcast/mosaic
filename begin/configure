#!/bin/bashs

if [ "$1" == '--user' ]; then
	INSTALL_MODE=user
else
	INSTALL_MODE=system
fi

cd begin

if [ "$INSTALL_MODE" == "system"]; then
	sudo ./configure_nginx.sh $*
fi

sudo ./configure_pgsql.sh $*
sudo ./configure_rbenv.sh $*
sudo ./configure_ruby.sh $*

cd ..